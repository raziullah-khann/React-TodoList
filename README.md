# 📝 React TodoList App (with LocalStorage)

A simple and responsive **TodoList application** built using **React.js (Vite)** and **LocalStorage** for saving data locally.  
This app allows users to **add, edit, delete**, and **persist** todos — even after refreshing the page.

---

## 🚀 Features

- ➕ Add new todo items (Title, Price, Description)
- ✏️ Edit and update existing todos
- 🗑️ Delete todos instantly
- 💾 Data persistence using browser **LocalStorage**
- ⚛️ Built with **React Hooks** (`useState`, `useEffect`, `useParams`, `useNavigate`)
- 🧩 Modular structure — each feature handled by a separate component
- 🎨 Clean UI styled using **CSS Modules**

---

## 🧠 Key Concepts Covered

- State management using React hooks  
- Form handling and validation  
- Routing with **React Router DOM**  
- Creating a **custom hook** (`useLocalStorage`) for persistent state  
- Synchronizing React state with browser LocalStorage  
- Reusable and maintainable component-based structure  

---

## 🏗️ Tech Stack

| Category | Technology |
|-----------|-------------|
| Frontend | React.js (Vite) |
| Routing | React Router DOM |
| Storage | LocalStorage |
| Styling | CSS Modules |

---

## ⚙️ Installation and Setup

Follow these steps to run the project locally 👇  

---
cd todolist-app
##Install dependencies
npm install
##Run the app 
npm run dev 

### 1️⃣ Clone the repository
```bash
git clone https://github.com/raziullahkhann/todolist-app.git
